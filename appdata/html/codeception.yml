# global codeception file to run tests from all apps
include:
  - frontend
paths:
  log: console/runtime/logs
settings:
  colors: true
coverage:
  enabled: true
  remote: false
  include:
    - /*.php
  exclude:
    - vendor/*
    - tests/*